# Findings

Internal audit findings from maintainer passes over the codebase. **This is not the
bug tracker** — if you hit a problem, please open an issue instead:
<https://github.com/new-usemame/Calibre-Web-NextGen/issues>.

These are things a maintenance pass noticed in passing and recorded so they are not
lost. They are unprioritised against user reports, and a user report always outranks
anything here. Generated by `scripts/findings.py index` — edit the JSON, not this file.

250 open, 38 closed.

## Open


### security

| id | area | finding |
|---|---|---|
| `F-9ca4a5` | container-init | python -m cps resolves via the editable .pth, so PYTHONPATH now outranks the application |
| `F-fc734f` | ingest | cwa-ingest-service evaluates a DB-sourced value in an arithmetic context (shell-eval primitive) |
| `F-f6a8bf` | spa | New UI grants whole-book delete to delete-without-edit users; classic requires both roles |
| `F-ed109c` | spa | The viewer role is inverted in the New UI reader: it blocks the users it exists for and admits the ones classic refuses |

### data-integrity

| id | area | finding |
|---|---|---|
| `F-1b2fdd` | ingest | Ingest can overwrite a good existing format when automerge is set to overwrite |
| `F-1fdb7c` | ingest | Ingest retry queue: at-least-once after a crash, and a non-atomic rewrite that can discard work |
| `F-7a1bf7` | ingest | Ingest conversion timeout is flat, not proportional to book size |
| `F-b521d3` | kobo | Kobo sync overwrites a further reading position with a device's older one (progress goes backwards) |
| `F-fe2b1c` | progress-sync | Lost update on the shared kosync row |
| `F-8ebeb7` | ingest | wait_for_stable_file can declare a stalled copy settled |
| `F-50a5cb` | editbooks | Cover writes replace the library file in place with no staging, so an I/O failure destroys the old cover |
| `F-579bf6` | kobo | Naming a ContentId in checkforchanges and then answering its GET with a 304 wipes that book's annotations on the device |
| `F-5c1146` | kobo | The annotations PATCH is answered success even when a member failed to commit |
| `F-a66189` | kobo | Annotations made outside a working capture window are unrecoverable, and no seed capture has ever run |
| `F-3e383a` | kobo | KEPUB repair may never reach a device that already has the book, and two places document a mechanism the code does not implement |
| `F-fed247` | kobo | The KEPUB backfill splits books that have already been on a Kobo, gated on a predicate that cannot see device highlights |
| `F-2fcc0d` | kobo | Two of the operator's real annotations are unrenderable on hardware: the #1657 fragment defect, measured in the wild |
| `F-a9b223` | kobo | Annotation backup retention is count-based and evicts the pre-loss snapshot, and no restore path exists to use one |
| `F-3db089` | spa | New UI can delete a book's last remaining format; classic deliberately prevents it |
| `F-567c75` | spa | Bulk delete announces every selected book deleted even when requests failed |
| `F-5ff379` | kobo | A last_modified bump makes a Kobo drop its downloaded copy (the onward orphaning claim is CORRECTED - it did not reproduce) |
| `F-c6b236` | api | SPA delete endpoints discard the deletion core's result and always return 204 |
| `F-0b66d7` | kobo | A Kobo deletion uploads on BOOK CLOSE, not on the library sync - a deletion made while the book stays open sits un-uploaded indefinitely |
| `F-66edbc` | kobo | CWNG's hidden=1 never reaches the reader, and the re-download GET proxied to Kobo hands the deleted row back (cause of the resurrection: #1923) |
| `F-e0d9fc` | frontend-e2e | SPA E2E secondary-user fixtures leak accounts when teardown loses its page or the runner is killed |

### correctness

| id | area | finding |
|---|---|---|
| `F-062a91` | spa | Reader save-retry: coalesced re-entry races the armed retry timer, and retries outlive the reader |
| `F-07fd40` | ingest | [bug] The whole app stops answering while a book is being ingested |
| `F-08f29f` | metadata | [bug] A metadata change log can sit unprocessed until an unrelated edit arrives |
| `F-0bd78b` | ingest | Books already in the ingest folder when the container starts are never imported |
| `F-0eed67` | kobo | Cover changes do not bust the browser cache outside the edit page |
| `F-151f59` | api | Read-status toggle routes still report success on a rolled-back write (#1318 contract gap) |
| `F-15488f` | spa | Reader save failures are invisible to sighted users; classic reader never retries in-session; SPA loses position on tab close |
| `F-194ce4` | metadata | Web-reader savepoint can roll back app.db while metadata.db stays committed |
| `F-1b7a81` | spa | [bug] SPA a11y gate is red on main: color-contrast (serious) on every light-theme route |
| `F-1f6174` | db | Duplicate visibility revalidation is recomputed per page render and per status poll |
| `F-205a8e` | kobo | Kobo: failed on-demand KEPUB conversion silently falls back to plain EPUB (book loses highlighting/annotations) |
| `F-22bb78` | kobo | CWA_COVER_PICKER_TIMEOUT is a dead knob — the cover-picker fan-out has no overall deadline |
| `F-2515ca` | koreader | Plugin publish is only partly resumable: a push that lands with no release still reports success |
| `F-2a34af` | core | X-Forwarded-Prefix reaches SCRIPT_NAME unvalidated: every consumer of request.script_root inherits it |
| `F-2be7fb` | spa | A perPage change after paging has advanced can query the old page number at the new size |
| `F-31a248` | ci | auto-revert workflow can't open PRs, so the red-main safety net produces nothing actionable |
| `F-3443ba` | metadata | Two more request-path time.sleep retries can freeze the gevent hub (web.py) |
| `F-347c16` | api | Bookmark routes can raise before any commit guard, leaving the shared session failed |
| `F-34a698` | ingest | test_ingest_batch_dirty fails on a clean main |
| `F-3aef89` | kobo | SPA and classic readers can land on different bookmark rows when the served format switches to kepub |
| `F-3eec38` | koreader | KOReader sync: "No book found for checksum" is undiagnosable from the log alone |
| `F-428248` | ci | CI: the Docker integration suite is dead — tests/docker/ shadows the Docker SDK |
| `F-43d21e` | autopilot | sync-wiki aborts on a wrong gh active user that the adjacent step reads correctly |
| `F-442557` | ingest | Ingest: a converter's child processes can outlive the conversion deadline |
| `F-447ab1` | kobo | Marking a book Read part-way through freezes a stale progress percentage beside the Read checkmark |
| `F-448e80` | ingest | test_script_import_side_effects is racy under xdist: the lockfile assertion fails on a different parameter each run |
| `F-4bd825` | ingest | Ingest: a failure after a successful conversion can still drop the book |
| `F-5024cc` | spa | Sorting: Å, Ä and Ö belong after Z for Swedish, Danish, Norwegian and Finnish readers |
| `F-5647b8` | spa | a11y.spec.ts reports a different failure set on every run, so the a11y gate cannot be trusted |
| `F-5ac530` | spa | [bug] Light theme fails WCAG contrast across the sidebar, and the SPA a11y gate is skipped on main |
| `F-5f6795` | kobo | Kobo deferred KEPUB download advertises the EPUB's byte size |
| `F-5f8b9c` | koreader | The KOReader plugin's Lua test suite has no CI runner |
| `F-5fa5a0` | spa | [a11y] Light theme fails colour contrast on 31 nodes — the a11y gate is red across most pages |
| `F-6d6dfd` | koreader | KOReader: the "couldn't read the local set" guard is inert on the default provider path |
| `F-738bba` | ci | Wiki sync has no CI gate, and repo/wiki-src is a divergent unpublished copy |
| `F-78cd70` | koreader | KOReader sync plugin: move releases to a dedicated repository |
| `F-8260d1` | koreader | KOReader sync can un-finish a book marked Read on a default install (the #1340 guard is web-reader-only) |
| `F-84e991` | api | refresh_for_new_data() only refreshes the calling request's session, not other in-flight ones |
| `F-85844d` | api | Integration tests on fork PRs: can a fork's GITHUB_TOKEN pull the private pbs-cache mirror? |
| `F-89b567` | koreader | KOReader bulk pull costs one blocking request plus a file hash per book, with no yield |
| `F-8bdb42` | spa | New UI: moving a shelf in the sidebar drags the separator line with it |
| `F-9073e5` | kobo | Reading-position 'furthest wins' is decided in Python, not by the UPDATE |
| `F-970750` | kobo | Custom-column mark-read never reaches the Kobo for a book with no prior reading state |
| `F-9fdee5` | spa | Finishing a book in the web reader marks it read for every user on a custom-read-column install |
| `F-9ff0a6` | admin | [a11y] View-settings popup promises a menu it does not provide |
| `F-a24ccb` | metadata | Bulk metadata edits leave stale cards in the library, and report success even when every write failed |
| `F-a3bf89` | i18n | auto-revert is a dead safety net, and would be dangerous if revived |
| `F-a7004f` | metadata | Metadata enforcement reports DONE and stores a checksum even when the calibre subprocess failed |
| `F-b095e3` | ci | CI: a failed (not skipped) changed_paths silently disables the build gate |
| `F-b363fe` | i18n | SPA i18n: fuzzy-flagged translations silently fall back to English across all locales |
| `F-b6019b` | spa | "Edit default view" opens a blank Advanced search form |
| `F-bcb5de` | koreader | KOReader: no pull prompt when the device wakes with the book already open |
| `F-cb615d` | spa | Comics/manga reader still pages left-to-right for right-to-left titles |
| `F-cbf3dc` | i18n | pt_BR: 415 fuzzy entries fall back to English, including "Book not found" and "No books found." |
| `F-cc7377` | metadata | Metadata edits are a silent no-op when enforcement is off — no indication anywhere in the UI |
| `F-cc8450` | kobo | Read state has more writers than the read toggle, and the two-carrier write is not atomic |
| `F-d1f997` | kobo | Version probes can hang the server if a binary is chatty (process_wait reads pipes after wait) |
| `F-d2a62a` | admin | Duplicates status: cache-missing response can't distinguish a running scan from 'needs a manual scan' |
| `F-d82680` | kobo | Ranker buries a shipped-then-broken issue: 'handled' outranks everything and 'pushback' is read by nothing (#324 is bucket 5) |
| `F-d96519` | i18n | extract_spa_strings.py --write drops ~30 msgids that are still live in .tsx |
| `F-db85a3` | ci | Triage the 37 tests that were red behind the CI marker gap |
| `F-dcc473` | kobo | Verify checksums on the Python and kepubify downloads |
| `F-dccd6c` | ci | CI: an integration suite that cannot run at all still reports the workflow green |
| `F-dde17d` | spa | Book covers fail silently when the session is gone: /cover/<id> redirects to the login page instead of answering an image |
| `F-de9cfb` | spa | SPA e2e: count-pinned specs break on a drifted seed, and mutating specs aren't isolated |
| `F-e23f3c` | koreader | Integration: 36 kosync tests skip because cwa_api_client cannot log in |
| `F-e3bdd7` | kobo | Clearing a series or tag still doesn't reach the .kepub (ebook-meta merges, ebook-polish replaces) |
| `F-e4da4d` | koreader | KOReader: deleting a large batch of highlights can time out — the delete path is unbounded on both sides |
| `F-e72fe3` | api | cps/redirect.py::remove_prefix strips the mount prefix without a segment boundary |
| `F-e9aee5` | koreader | The KOReader plugin's Lua tests never run in CI |
| `F-ed2450` | i18n | tier-1 auto-merge validation does not enforce the "no code" clause of its own tier definition |
| `F-f38008` | kobo | KOReader sync: a mismatched content_id makes a zero-row Kobo read look like an emptied device |
| `F-f3c4df` | ci | test_1094 conversion-deadline test asserts against ambient wall-clock, flakes under load |
| `F-ff3809` | ingest | Count messages have no plural form: English reads "1 books", and inflected languages cannot render them at all |
| `F-065a9a` | cover-picker | Cover picker fan-out omits copy_current_request_context, silently dropping per-user Hardcover tokens and all DNB language data |
| `F-123292` | oauth | Cancelled or failed OAuth loops back to the provider instead of the login page |
| `F-27fa63` | container-init | Non-root container start leaves three real residual failures (ImageMagick policy, cps/cache, dirs.json) and the 0-EPERM claim is a Docker Desktop fakeowner artifact |
| `F-391ce8` | cwa-functions | download_current_log siblings rewrite their own abort(403)/abort(404) as 400 |
| `F-4229fc` | autopilot | verification-sweep discards gh's stderr, so the regression radar's failure reason is unrecoverable |
| `F-5d9298` | container | metadata-change-detector starts two watchers as root while its sibling drops to abc |
| `F-5d5d5c` | ci | preflight-release-tag.sh is a correct gate with no caller — invocation depends on prose in LOOP.md |
| `F-6e4ea6` | ci | findings/INDEX.md is generated and committed, so concurrent findings PRs always conflict |
| `F-79d746` | ci | TIER1_PATHS_REGEX is declared as tier policy but enforces nothing |
| `F-b6a4af` | container | cwa-auto-library swallows its own exit code, so a failed boot step reads as success |
| `F-27c656` | auth | Global limiter key_func is the literal True, so any rate limit added without an explicit key_func 500s |
| `F-438052` | cwa-db | add_missing_setting treats a lost duplicate-column race as a failed migration |
| `F-5d88fa` | scripts | scripts/ still hardcodes /config for processed_books, cwa.db and ingest batch markers |
| `F-5f4977` | kosync | CWA_DB's SystemExit escapes the fail-closed guard in progress_syncing/settings.py |
| `F-71ff3d` | web | Health probe and book count turn any exception into a plausible wrong answer |
| `F-7ca150` | web | Health check SELECT 1 never reads metadata.db, so a corrupt library probes healthy |
| `F-9d6b51` | autopilot | Load-sensitive fingerprint flake in test_tick_lock.sh silently kills whole ticks |
| `F-af3ee7` | i18n | messages.pot is stale, so ~198 real user-facing strings are invisible to every translator |
| `F-f6f411` | testing | test_ingest_batch_dirty hot-loop test fails locally but is green in CI |
| `F-33d694` | autopilot | awaiting-verification sweep counts a third party's comment as the reporter verifying |
| `F-54809a` | autopilot | findings.py --force keys on the title, so an edited title silently forks a second finding |
| `F-561a35` | autopilot | Preflight failure diagnostics are overwritten by the next successful tick |
| `F-56992b` | auth | MAX_LOGIN_REDIRECTS=3 permits four automatic OAuth starts |
| `F-66d002` | autopilot | completed-log self-heal is blind to a missing timestamp, the defect that wedges preflight |
| `F-83cd73` | i18n | extract_spa_strings.py --write silently discards hand-added anchors |
| `F-973e7a` | autopilot | Issues closed by the #1391 migration read as shipped; findings migrated from them read as open |
| `F-b09b8e` | ingest | convert_library and kindle_epub_fixer avoid the import-time lock but cannot reclaim one from a killed run |
| `F-d85180` | autopilot | findings/INDEX.md has no consistency gate, so it drifts silently and is repaired by hand each time |
| `F-0b6d6c` | autopilot | Phase-7 mutates other sessions' branches in the shared repo/ checkout |
| `F-1760ca` | ci | withdrawing merge authorisation does not disable auto-merge |
| `F-436d41` | autopilot | The orphaned-PR heuristic counts rollup state, not required-check presence, so it reports PRs that can never merge |
| `F-47a610` | ci | auto-merge arms without pinning the validated head (TOCTOU) |
| `F-81d428` | ci | Test Suite Summary reports success when a required job was CANCELLED — it tests for failure, never for success |
| `F-035fe4` | api | The server drops connections under parallel e2e load, failing tests transport-level |
| `F-237e67` | release | The release-tag gate is invoked only by prose, so a tick can simply not run it |
| `F-29b691` | reader | Search rejects one-character queries, which are real words in CJK |
| `F-389161` | ci | safe-tier-1's path allowlist was loaded but never enforced |
| `F-7786b7` | ci | The :dev image publishes from commits whose Test Suite never passed |
| `F-08240d` | triage | The follow-up board's 48h grace hides an explicit request for a human |
| `F-222645` | autopilot | A contributor PR that is red is invisible to the follow-up board |
| `F-304cf4` | db | db.py: fill_indexpage and get_search_results put defaulted params before *args |
| `F-639366` | outreach | The outreach dupe-guard makes self-correction impossible on any thread we got wrong |
| `F-6a005a` | autopilot | A rule-6 hold has no delivery step, so the escalation never reaches the operator |
| `F-8d8c15` | autopilot | Follow-up board is blind to PRs, so a waiting contributor is invisible to it |
| `F-aea57f` | autopilot | An armed auto-merge that cannot merge is exempted by every repair path |
| `F-2f55a0` | testing | Four unit tests fail on a clean local checkout but pass in CI |
| `F-7c3cd9` | container-init | Dropping the s6 chdir moves relative stored paths from the app root to /config |
| `F-893659` | metadata-editor | Typeahead query interpolates the raw term into a LIKE pattern, so % and _ act as wildcards |
| `F-e3e71a` | db | DESKTOP_COMPAT_MODE logs 'WAL mode remains disabled' without ever converting an existing WAL library back to DELETE |
| `F-720dec` | release | KOReader plugin publish has failed on every release since v4.1.33, and the pre-tag gate cannot see why |
| `F-2810de` | infra | metadata_db_write_lock hard-codes /config, so callers break on installs without it |
| `F-34b63b` | web-reader | The SPA reader's note composer repaints a recolour it never persists |
| `F-4bb29a` | kobo | A malformed reading-state LastModified is stored as an invented server clock |
| `F-8efa11` | web-reader | The legacy reader's safeColorToken can strip an unknown colour down to a palette name |
| `F-724452` | testing | Correction: my annotation-store measurements from 2026-08-18 do not reproduce |
| `F-adaa84` | ci | The reverse-proxy sub-path e2e suite has never run in CI |
| `F-cc5efb` | testing | Source-pinned tests promise runtime behaviour they never execute, and at least one pinned claim is false |
| `F-2f48b9` | spa | Duplicates sidebar link is gated on the wrong role bit, failing in both directions |
| `F-57c90e` | spa | New UI Tasks drops the start time and the failure message it already receives |
| `F-b19131` | spa | New UI Tasks renders the server's HTML task message as literal text |
| `F-f61640` | spa | New UI Tasks has no equivalent of classic's two scheduled-work queues |
| `F-cb28e5` | spa | A rating-type custom column renders a second control with the identical accessible name 'Rating' |
| `F-33dea0` | covers | book_cover_is_locked fails OPEN, so a failing lock query silently permits the overwrite it exists to prevent |
| `F-493b49` | ci | cancel-in-progress on main discards ~80% of Test Suite runs before they dispatch, so most commits land unverified |
| `F-bd1030` | test | Every local rig runs NETWORK_SHARE_MODE=false; production runs true, so no local test exercises prod's transaction mode |
| `F-d8e809` | ci | The identity firewall blocks forward-merging main, so a PR whose base moves can never satisfy the strict up-to-date rule |

### ux

| id | area | finding |
|---|---|---|
| `F-098b15` | i18n | New UI is largely untranslated in every language except Russian |
| `F-151255` | spa | New UI: a save error throws away what you typed and restores the old value |
| `F-188930` | kobo | Set a custom Kobo sleep-screen / background from the web UI |
| `F-209826` | core | sp1-ux.spec.ts:3 is flaky (nav ordering assertion) |
| `F-3605ee` | metadata | Centralize metadata-provider HTTP timeout budget (consistency) |
| `F-3d40e4` | spa | New UI: List smart shelves on /shelves + consistent back-links |
| `F-415292` | spa | New UI reader: no swipe-to-turn-page on touch devices (classic has it) |
| `F-5384f4` | spa | [New UI] Make the series line on book cards a link to the series |
| `F-5c7d88` | api | Scoped, revocable API tokens for third-party clients |
| `F-63e69a` | ci | CI: a Dockerfile-only PR doesn't run the Docker integration tests |
| `F-64f2bd` | spa | Convert on book page v2: common conversions first, 'Other…' for custom targets |
| `F-794ff3` | koreader | Import highlights from a Kindle/KOReader `My Clippings.txt` file |
| `F-a3bcff` | spa | Ambient animated backgrounds for the library and reader |
| `F-b22824` | kobo | Consider defaulting new users to "sync only selected shelves" for Kobo |
| `F-d60c09` | spa | New UI: Duplicate action for ordinary shelves (parity with smart shelves) |
| `F-d9392a` | metadata | Book detail page scrolls sideways on mobile when a metadata value has no spaces |
| `F-e12078` | metadata | Add Anna's Archive as a metadata provider |
| `F-e46f22` | spa | New UI: show highlight count badge on the Highlights button |
| `F-e64448` | ingest | Ingest: actually fulfill .acsm tickets instead of only explaining why conversion failed |
| `F-feec51` | metadata | Hardcover title-search covers stay low-res when the record has no ISBN |
| `F-2f6258` | packaging | A missing startup-critical dependency now fails with a raw traceback instead of a named install instruction |
| `F-1a7d85` | spa | New UI hides the smart-shelf Kobo toggle instead of explaining why it is unavailable |
| `F-30e824` | kobo | KOReader highlight sync is hardware-proven, shipped, and advertised in neither UI |
| `F-3e4596` | docs | Our 2026-05-19 comment on janeczku#2610 answers a question the thread is not asking |
| `F-5a225c` | spa-topbar | The mobile hamburger renders no glyph at 320px |
| `F-6f88a0` | spa-error-states | A nonexistent entity renders its error as the entity's own name |
| `F-7765d1` | spa | BookCard bypasses the Button primitive and dresses the edit control in cover-overlay tokens |
| `F-8f8a5b` | spa-bookcover | A cover that fails to load shows browser alt text, not the designed fallback |
| `F-a4eae9` | spa-layout | Two routes overflow horizontally at 320px |
| `F-b443cb` | opds | OPDS catalog root still shows four English strings to a German user |
| `F-c5bbe7` | spa-error-states | The library's failure state is the raw server error string in an empty-state shell |
| `F-f1159f` | spa | SPA ordinary-user parity gap is systemic, not isolated — ~30 classic controls have no SPA equivalent |
| `F-4079f1` | i18n | Dutch renders the Compact and Dense density options identically |
| `F-dd086d` | i18n | Two SPA msgids sharing one translation: a good diagnostic, a bad gate — 222 hits, a handful real |
| `F-fee47e` | reader | Reader highlight styling drops epub.js mix-blend-mode; under-highlight contrast unmeasured on sepia |
| `F-6543d1` | spa | Nine decorative lucide icons in the SPA carry neither aria-hidden nor focusable={false} |
| `F-bd6680` | spa | Per-user notices are SPA-only, so a classic-fallback user never sees KEPUB repair instructions |
| `F-f8fdc9` | spa | The native reader's scroll container is not keyboard-reachable, so TXT is unscrollable by keyboard on Safari |

### perf

| id | area | finding |
|---|---|---|
| `F-075496` | kobo | Every KEPUB download rewrites the whole zip on the request greenlet when embed_metadata is on |
| `F-5c2758` | kobo | EPUB layout cache does not remember failures, so a corrupt book re-parses and re-logs on every sync page |
| `F-652e6e` | kobo | Kobo store proxying blocks the request greenlet for up to 12s per non-GET call |
| `F-a6c145` | kobo | Kobo cover padding runs ImageMagick on the request greenlet on every cache miss |
| `F-c577f9` | kobo | Kobo sync re-parses every book's EPUB container+OPF from disk on every sync page |
| `F-c96bd6` | kobo | Kobo sync commits once per emitted book inside the request |
| `F-3abc47` | reader | The search hit cap does not bound work inside a single section |

### test

| id | area | finding |
|---|---|---|
| `F-15d425` | spa | Follow-up board files reporter confirmations as "we owe a reply" (5 of 8 rows are not owed) |
| `F-2b5186` | spa | Magic-link disabled path (403) has no automated coverage |
| `F-4939f6` | ci | hidden-books.spec.ts:237 flakes on 'Response has been disposed' (shared route.fulfill pattern) |
| `F-9c44aa` | spa | Book-card action row: three gaps in the #1166 e2e coverage |
| `F-abdea7` | spa | Promote the SPA e2e gate to required once it has earned it |
| `F-d4107c` | ci | API tests bypass the HTTP boundary: routing, method and auth decorators aren't pinned |
| `F-7a530b` | ci | tag-merge-delete.spec.ts flakes on 'socket hang up' and can block a release cut |
| `F-d16bdf` | ci | Fork PR with CI stuck in action_required reads as green to the merge gate |
| `F-f47b55` | test | test_wait_for_stable_file_reports_failure_when_never_stable is timing-flaky under xdist |
| `F-b24b81` | ci | Update Translations commits [skip ci], so a generated .po change can red main with zero CI signal |
| `F-08685b` | testing | e2e specs share a 3-book seeded library under fullyParallel, so concurrent specs clear each other's fixtures |
| `F-444a5f` | testing | test_enforcer_missing_log_logs_single_info_not_warning_spam is red on main |
| `F-54d342` | ci | the SPA e2e suite never runs on main, so a regression from two independently-green PRs is invisible until an unrelated PR trips it |
| `F-777bf4` | testing | Pre-existing red: test_802 enforcer_missing_log fails locally while CI reports main green |
| `F-8b3e17` | tests | test_1094 TestConversionDeadline fails only under -n auto, and passes in isolation on both the branch and main |
| `F-36a362` | tests | Deadline/timeout unit tests fail under full-suite load, never in CI |
| `F-409038` | ci | The CI path filter treats .dockerignore as non-build-affecting, so the Docker lane skips the one file that always is |
| `F-894f7d` | ci | preflight never checks main's CI, so a tick can drain onto a red main |
| `F-d14ef8` | ci | e2e 'socket hang up' is metadata-change-detector spawn storm starving gevent, not a crash |
| `F-7ebe6d` | testing | test_1596_module_entry_point silently requires an editable install |
| `F-b30ec2` | testing | Unit tests write annotation-backups/ into the repo root |
| `F-40a954` | testing | Running the Docker integration suite locally silently tests UPSTREAM's image, not the branch |
| `F-7bbdd6` | ci | A stalled apt step burns the whole fast-tests budget and then reads as a TEST failure |
| `F-65c6db` | test | Fifteen tests break when TMPDIR contains a space, because the test harnesses interpolate paths into shell contexts unquoted |
| `F-a114f8` | test | Mutation testing by hand has three failure modes that all report as a PASS |

### docs

| id | area | finding |
|---|---|---|
| `F-df9d72` | docs | Install guides: add real-hardware-verified screenshots |
| `F-bd9d84` | progress-sync | kosync protocol reference lives outside the wiki-generated docs set |
| `F-79cac0` | docs | README's Kobo troubleshooting command shows nothing on a default install |
| `F-97846a` | docs | Seven install guides are written but have never reached the wiki: the autopilot renders from a stale untracked copy |

### chore

| id | area | finding |
|---|---|---|
| `F-0504c5` | shelves | Magic-shelf pagination: full cursor (magic_shelf_last_id residual) |
| `F-1a7b43` | i18n | cps/spa_strings.py AUTOGEN block has drifted ~57 msgids from the SPA source |
| `F-c95945` | ci | tier-1 auto-merge stalls forever once a PR goes BEHIND |
| `F-2fe401` | autopilot | Tier-2 auto-merge cap counts test files, so writing regression tests demotes your own PR |
| `F-537fa8` | autopilot | phase7-envelope truncates model output with cut -c, which counts bytes under launchd's C locale |
| `F-ff7a34` | frontend-e2e | Local SPA e2e suite is red and flaky on main (13-14 failures, shifting set) |
| `F-8cc0a0` | docker | Deleting a COPYed path in a later RUN layer never reclaims image bytes |
| `F-f30e75` | tooling | gitignore's trailing-slash node_modules rule does not match a symlinked node_modules |
| `F-a247e9` | logging | logger.py carries a dead sys.version_info > (3, 7) branch |
| `F-f5f00a` | ci | Merging a .po PR auto-generates a main commit that puts every other PR BEHIND, so serial translation merges each cost a full refresh + CI + fork-approval cycle |
| `F-50a97d` | ci | E2E resolver waits its full ~2h budget even when the paired dev-image build has already failed |


## Closed

| id | status | finding | resolved |
|---|---|---|---|
| `F-011141` | resolved | user.locale is stored unvalidated and reaches get_locale() verbatim | 2026-08-29 |
| `F-277165` | resolved | Kobo cover lookup by UUID bypasses per-user visibility filtering | c339e529 |
| `F-2ff8ac` | duplicate | KOSync furthest-wins is an unlocked read-compare-write, so a concurrent push can regress progress | 2026-08-07 |
| `F-3b565b` | resolved | Kobo annotations GET is proxied to Kobo's cloud — unexamined data-loss exposure (CW #2610) | d7e485d2 |
| `F-0eb843` | resolved | The KOReader cwasync plugin carries the same wrong Kobo colour table the importer had | 1c215cbc |
| `F-30d264` | resolved | checkforchanges containment is bypassed when Kobo sync is off or the session is unauthenticated | 2026-08-22 |
| `F-5769c9` | resolved | The KoboReader.sqlite import assigns the wrong highlight colour, and every Clara BW highlight imports as yellow | a751f648 |
| `F-7e418c` | resolved | The KoboReader.sqlite importer never sets annotation_type, so every recovered row is typed NULL | d8fa6626 |
| `F-b1004e` | resolved | The KoboReader.sqlite import drops every dogear and note-only bookmark before it is counted | d8fa6626 |
| `F-e4e119` | resolved | The KoboReader.sqlite import reports rows imported after a rolled-back commit | 08a44a98 |
| `F-1927e0` | resolved | A KOReader push can flip an existing annotation's source and then delete it, defeating the delete boundary the code claims to enforce | 2026-08-21 |
| `F-947bb8` | resolved | A device-supplied timestamp can silently destroy a highlight: OverflowError escapes every ISO clock parser | 4e8e6881 |
| `F-a54711` | resolved | Kobo PATCH deletedAnnotationIds has no provenance authority: one push soft-deletes webreader and koreader rows | 39da46bf |
| `F-a7b6b2` | resolved | Duplicate-book merge hard-deletes the newer annotation with no recency check, while reading state in the same file keeps the newer | f8cac66d |
| `F-48fca9` | resolved | Typing the literal word "None" into a custom column clears it instead of saving it | v4.1.25 |
| `F-571ee7` | resolved | GET /robots.txt returns 404 in every deployment — needs a crawl-policy decision | v4.1.21 |
| `F-9c171e` | resolved | Surface Hardcover token status in admin | v4.1.11 |
| `F-a0d61c` | resolved | Community translation PRs still go CONFLICTING when they remove a fuzzy flag | 2697951b |
| `F-e63807` | resolved | Hardcover token status + env-token note are hidden until "Enable Hardcover Sync" is on | v4.1.14 |
| `F-65c93f` | resolved | SPA cover endpoint ignores the cover lock and 404s on the user's own hidden or archived book | 8cf36699 |
| `F-e628c6` | resolved | The KoboReader.sqlite import is insert-only and dedups on the wrong key | d8fa6626 |
| `F-178d9a` | resolved | Spine splitter cannot separate chapters whose TOC anchors nest inside one another | b0fb9749 |
| `F-9de049` | resolved | annotation_type has five writers, no owner, and no agreed vocabulary | 9e06f0fa |
| `F-bbd10e` | resolved | The do-not-split-annotated-books guard inverts for a book that was born split | 6cc28e6b |
| `F-6999cf` | resolved | Adding a column to the User model breaks every earlier migration that ORM-queries User | 2026-08-29 |
| `F-b649f0` | resolved | Shelving a book you do not own is a server-side 'invalid Book Id'; the documented add-to-library-first rule lives only in the SPA client | 2026-08-29 |
| `F-0a2f72` | resolved | New UI: read/unread + favorite quick-actions on book cards (parity with classic cards) | 2026-08-06 |
| `F-ab1edf` | resolved | Move writable runtime directories (cps/cache, metadata_change_logs, metadata_temp) out of the application tree | v4.1.31 |
| `F-b198f4` | resolved | Hardcover has two enable flags in two databases; reconcile them, then allow enabling by env var | v4.1.14 |
| `F-b82071` | resolved | Hardcover: log token presence + source, and distinguish HARDCOVER_TOKEN from HARDCOVER_TOKEN_FILE | v4.1.14 |
| `F-c78e8c` | resolved | New UI: Kobo-sync toggle for smart shelves (backend already wired) | v4.1.19 |
| `F-0b8c7b` | resolved | One concept, three names: 'Magic Shelves' (classic) vs 'Smart shelves' (new UI) vs magic_shelf (API) | 2026-08-09 |
| `F-88da18` | resolved | Fixes ship but the originating issue never gets its close-out, so reporters sit on solved problems | 1cf1db3 |
| `F-7d206e` | resolved | SPA loading spinners: 4 of 7 honour prefers-reduced-motion, 3 do not (incl. the shared Spinner) | 202b8842 |
| `F-062863` | wontfix | New UI TXT reader has no paging; classic paginates with arrows, keys, swipe and wheel |  |
| `F-9032c8` | resolved | cover_enforcer.py takes an import-time lock released only by atexit, so a killed process reddens every later test run | 855d7043 |
| `F-943f51` | resolved | reader-notes repaint-after-reload is flaky and reds unrelated PRs | d239befa |
| `F-67982f` | resolved | Account page: email body template field missing | v4.1.11 |
